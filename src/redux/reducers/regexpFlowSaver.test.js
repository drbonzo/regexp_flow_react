import regexpFlowSaver from './regexpFlowSaver';
import {SAVE_REGEXP_FLOW} from '../actions';

describe('inputText reducer', function () {

    // FIXME
    it('FIXME', function () {
        expect(true).toBe(true);
    });

    // it('should return empty string when current state is undefined and action is other', function () {
    //     let state = undefined;
    //     let newState = regexpFlowSaver(state, {type: 'OTHER', text: 'aaaa'});
    //     expect(newState).toBe('');
    // });
    //
    // it('should not change state when action is other', function () {
    //     let state = 'foobar';
    //     let newState = regexpFlowSaver(state, {type: 'OTHER', text: 'not foobar'});
    //     expect(newState).toBe('foobar');
    // });
    //
    // it('should return text from action if action is UPDATE_INPUT_TEXT', function () {
    //     let state = 'foobar';
    //     let newState = regexpFlowSaver(state, {type: SAVE_REGEXP_FLOW, text: 'not foobar'});
    //     expect(newState).toBe('not foobar');
    // });
});
