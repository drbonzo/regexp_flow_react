class FilterConfig {

    constructor(filterType) {

        this.filterType = filterType;

        this.id = null;
        this.enabled = true;
        this.description = '';

    }
}

export default FilterConfig;
